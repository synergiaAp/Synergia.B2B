@*@model DetailedAnalysisViewModel
@Scripts.Render("~/bundles/detailedAnalysis/detailedAnalysisList")

@{
    ViewBag.Title = "Analiza szczegółowa";
}

<div class="row">
    <div class="col-lg-12">
        <div class="card">
            <div class="card-header card-header-text" data-background-color="blueSynergia">
                <h4 class="card-title">Lista szczegółowej aktywności użytkowników</h4>
            </div>
            <div class="card-content">
                <div class="col-md-4">
                    <div class="form-group label-floating">
                        @Html.Label("Użytkownik", new { @class = "control-label datepickerLabel", @for = "UserFilter" })
                        @Html.DropDownList("UserFilter", Model.Users, "Wszyscy", new { @class = "form-control" })
                    </div>
                </div>
                <div class="col-md-4">
                    <div class="form-group label-floating">
                        @Html.Label("Kolumna", new { @class = "control-label datepickerLabel", @for = "ColumnFilter" })
                        @Html.DropDownList("ColumnFilter", Model.Columns, new { @class = "form-control" })
                    </div>
                </div>
                <div class="col-md-2">
                    <div class="form-group label-floating">
                        @Html.Label("Od początku", new { @class = "control-label datepickerLabel", @for = "StartDateFilter" })
                        @Html.TextBox("StartDateFilter", "", new { @class = "form-control monthpicker" })
                    </div>
                </div>
                <div class="col-md-2">
                    <div class="form-group label-floating">
                        @Html.Label("Do końca", new { @class = "control-label datepickerLabel", @for = "EndDateFilter" })
                        @Html.TextBox("EndDateFilter", "", new { @class = "form-control monthpicker" })
                    </div>
                </div>
                <div class="material-datatables">
                    <table id="detailedAnalysisTable" class="table table-striped table-no-bordered table-hover dataTable" cellspacing="0" style="width:100%; display:none">
                        <thead>
                            <tr>
                                <th>Użytkownik</th>
                                <th>Rok / Miesiąc</th>
                                <th>Wartość w wybranej kolumnie</th>
                            </tr>
                        </thead>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>*@
